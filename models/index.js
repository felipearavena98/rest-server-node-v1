const Category = required('./category');
const Role = required('./role');
const Server = required('./server');
const User = required('./user');
const Product = required('./produc');

module.exports = {
    Category,
    Role,
    Server,
    User,
    Product
}